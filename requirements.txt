fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
python-dotenv==1.0.1
apscheduler==3.10.4
markdownify==0.13.1
beautifulsoup4==4.12.3
readability-lxml==0.8.1
lxml==5.1.0
pydantic==2.9.2
cachetools==5.5.0
mcp==1.2.1

# ===== x402 공식 결제 레이어 (app/payment.py) =====
# web3==6.20.3는 자체 온체인 검증 코드(app/payment.py)를 걷어내면서 더 이상
# 필요 없어져 제거했다 - 결제 검증/정산은 이제 아래 두 패키지가 담당한다.
# 버전은 2026-09 기준 PyPI 최신(x402 2.21.0)으로 맞췄다 - 이 샌드박스는 외부
# 네트워크가 막혀 있어 실제 설치/실행 테스트는 못 했으니, 설치 시 버전 충돌이
# 나면 `pip install -U x402 cdp-sdk`로 최신 버전을 다시 받아보길 권장한다.
x402[fastapi]==2.21.0
cdp-sdk>=0.10.2
